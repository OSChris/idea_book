%h1 Listing ideas
%table
  %thead
    %tr
      %th Title
      %th Body
      %th{colspan: "3"}
  %tbody
    - @ideas.each do |idea|
      %tr
        %td= link_to idea.title, idea
        %td= idea.body
%br
= link_to 'New Idea', new_idea_path
